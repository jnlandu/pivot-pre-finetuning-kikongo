[project]
name = "pivotkk"
version = "0.1.0"
description = "Pivot pre-finetuning for English->Kikongo MT"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
